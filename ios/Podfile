platform :ios, '13.0'

target 'Runner' do
  use_frameworks!

  # التبعيات المشتركة
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  pod 'Firebase/Analytics'
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  
  # أضف هذه السطر بدلاً من pod 'connectivity_plus'
  pod 'connectivity_plus', :path => '.symlinks/plugins/connectivity_plus/ios'
end