platform :ios, '13.0'

target 'Runner' do
  use_frameworks!

  # التبعيات المشتركة
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  pod 'Firebase/Analytics'
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'connectivity_plus'

  # التبعيات الخاصة بكل فلافر
  if ENV['FLAVOR'] == 'dev'
    # التبعيات الخاصة بفلافر التطوير
  elsif ENV['FLAVOR'] == 'prod'
    # التبعيات الخاصة بفلافر الإنتاج
  end
end
